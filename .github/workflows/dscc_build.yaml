name: DSCC Apps Package Build

on:
  workflow_call:


jobs:
  build:
    name: Build DSCC Tarball

    runs-on: [self-hosted, onprem-platform]

    steps:
      - name: Dump Environment Variables for Debugging
        run: echo "hello"
